[package]
name = "samara-api-sketch"
version = "0.0.0"
edition = "2024"
publish = false

[workspace]

[dependencies]
tokio = { version = "1.45.1", features = ["macros", "rt-multi-thread", "sync", "time"] }
